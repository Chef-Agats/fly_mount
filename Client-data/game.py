Arat

		if self.enableXMasBoom:
			self.__XMasBoom_Update()
            
            
ALTINA EKLE


		if app.UCAN_INEK:
			if app.IsPressed(app.DIK_LCONTROL) and app.IsPressed(app.DIK_F10):
				chrmgr.Ucanýnek(5.0)
			elif app.IsPressed(app.DIK_LCONTROL) and app.IsPressed(app.DIK_H):
				chrmgr.Ucanýnek(-5.0)